"""中文提示词模板 - 六层架构（Sandbox Mode）"""

# 第一层：设定层 (World Building & Lore) - 必须输出JSON
WORLD_BUILDING_PROMPT = """你是一位专业的世界观构建师。请**根据用户需求**，构建一个完整、一致的世界设定基础。**必须输出纯JSON格式**，总约200-300字。

用户需求：
{user_input}

请输出一个JSON对象（不要```json标记，直接输出）。**每个字段的值必须是你根据用户需求实际生成的具体设定内容**，绝不能照抄下面的结构说明：

{{
  "world_foundation": "你生成的世界观内容：物理规则、社会体系、地理环境等",
  "characters": "你生成的角色元数据：各主要角色的姓名、外貌、性格核心、能力、背景",
  "race_class_system": "你生成的种族/职业系统（如不适用可写\"无\"）",
  "constraints": "你生成的约束：故事必须遵循的规则、禁止的设定等"
}}

重要：上述仅为字段说明，你必须为每个字段填入**基于用户需求创作的真实设定文本**，不可输出「世界观基础：...」「角色元数据：...」这类标签式占位符。
"""

# 第二层：故事层 (Plot & Character Arc) - 必须输出JSON
STORY_LAYER_PROMPT = """你是一位专业的剧情架构师。基于已构建的世界设定，设计一个完整的故事骨架和人物弧光。当前运行模式为 **沙盘模式**：系统会在给定约束内自主推进故事，不假定作者会在中途纠偏。**必须输出纯JSON格式**，便于程序解析。

世界设定：
{world_setting}

用户需求：
{user_input}

请输出一个JSON对象，严格遵循以下结构（不要添加```json等标记，直接输出JSON）：

{{
  "core_theme": "核心主题与主线：故事主题、主要冲突线、基本调性",
  "character_arcs": "人物弧光：各角色的起始状态、成长轨迹、最终状态、行为模式变化",
  "episodes": [
    {{"stage": "开幕", "title": "本阶段标题（如：世界观与人物引入（第1-6集））", "outline": "本阶段详细梗概，含各集要点"}},
    {{"stage": "发展", "title": "本阶段标题", "outline": "详细梗概"}},
    {{"stage": "高潮", "title": "本阶段标题", "outline": "详细梗概"}},
    {{"stage": "结局", "title": "本阶段标题", "outline": "详细梗概"}}
  ],
  "foreshadowing": "伏笔与呼应设计"
}}

要求：
- episodes 数组按开幕 发展 高潮 结局顺序，每阶段一个元素
- stage 只能是 "开幕" "发展" "高潮" "结局" 之一
- outline 包含该阶段各集的详细梗概，可含第N集子标题
- 优先追求内部因果一致、长期回收潜力和阶段递进
- 除非用户需求明确要求，不要为了制造震撼而随意让核心人物死亡、永久失踪或发生难以收回的突变
- 只输出JSON，不要其他说明文字
"""

# 第三层：Episode层 - 链式生成：设定 + 核心主题与人物弧光 + 当前故事 + 上一个episode → 当前episode
EPISODE_LAYER_PROMPT = """你是一位专业的剧本章节师。根据**故事设定**、**核心主题与人物弧光**、**当前故事**和**上一个Episode**，扩写**当前Episode**。当前是 **沙盘模式**，你需要让故事自己往前生长，但不能依赖作者事后补洞。每次只生成一个Episode的详细概括。

故事设定（核心）：
{setting_core}

核心主题与主线（来自故事层）：
{core_theme}

人物弧光（来自故事层）：
{character_arcs}

当前故事（整体大纲与Episode梗概）：
{story_outline}

上一个Episode（若为第一个Episode则此项为空）：
{previous_episode}

当前需要扩写的Episode梗概：
{episode_outline}

请扩写为**详细概括**（300-500字），包含：场景与时空、人物与行动、关键对话要点、情感与氛围、叙事目的。
与上一个Episode衔接，为下一个Episode做铺垫。
- 尽量保留重要人物后续的叙事价值；除非上位信息明确要求，不要轻率写死、写废或写成无法挽回的关系断裂
- 可以埋伏笔，但伏笔应服务于故事自身的后续展开，而不是依赖外部作者额外解释

**必须输出纯JSON格式**（不要```json标记，直接输出）：
{{
  "title": "本Episode标题",
  "summary": "300-500字的详细概括全文"
}}
"""

# 第四层：Beats层 - 将Episode层的详细概括拆成多个Beats，完整精炼讲完本Episode
BEATS_LAYER_PROMPT = """你是一位专业的剧本分解师。你的任务：根据**Episode层的详细概括**，把本Episode拆成多个Beats，每个Beat完整精炼地讲述一部分，串联起来讲完整个Episode的故事。当前是 **沙盘模式**，Beat 需要服务于连贯推进，而不是靠临时的大转折破坏长期结构。Beats数量不限，直到本Episode内容完整覆盖。

设定核心：
{setting_core}

当前Episode（Episode层输出的详细概括，需拆分为Beats）：
{episode_context}

上一个Beat（本Episode已生成的Beats中的**最后一个**；若为第一个Beat则此项为空）：
{previous_beat}

请输出**下一个Beat**，严格为单个JSON对象，格式如下：

```json
{{
  "scene": "EXT./INT. 地点 - 时间",
  "environment": "环境描写",
  "action": "人物的动作与反应",
  "dialogue": [{{"角色名": "（动作/情绪）：\"对话内容\""}}],
  "purpose": "本Beat的叙事目的"
}}
```

要求：
1. 若上一个Beat为空，则这是本Episode的第一个Beat，请开篇
2. 根据上一个Beat自然承接，保持叙事连贯
3. 当本Episode内容已完整覆盖、无需更多Beat时，将scene设为 "[EPISODE_END]" 表示结束
4. 若涉及伏笔、秘密、人物关系变化，请以“可持续推进”为原则，不要做难以收回的硬断裂
5. 只输出一个JSON对象，不要其他文字
"""

# 第五层：文字层 (Textualization) - 仅用：上一个文字 + 当前Beat + 故事层核心 + 设定核心
TEXTUALIZATION_PROMPT = """你是一位文笔精湛的小说家。根据**上一个文字**、**当前Beat**、**故事层核心**和**设定核心**，渲染本Beat的小说正文。当前是 **沙盘模式**：正文应让读者感觉故事在自行演化，但仍需保留长期连贯和后续空间。

设定核心：
{setting_core}

故事层核心：
{story_core}

当前Beat（剧本格式）：
{beat_content}

上一个文字（若为第一个Beat则此项为空）：
{previous_text}

请将当前Beat渲染为小说正文，要求：

1. **文笔要求**
   - 语言流畅优美，将environment展开为细腻的环境描写
   - 将action展开为具体的动作、表情、心理描写
   - 将dialogue中的对话自然融入叙述，可加入说话时的神态、动作

2. **连贯性**
   - 与上一个文字自然衔接（若有）
   - 实现Beat中purpose所要求的叙事目的
   - 严格遵守设定核心与故事层核心
   - 若出现伏笔或异常细节，写得自然可感，但不要过早解释完

3. **字数**
   - 约200-500字，开篇有场景感，结尾自然过渡

请直接输出该Beat对应的完整文字内容，不要添加额外的说明或注释。
"""
